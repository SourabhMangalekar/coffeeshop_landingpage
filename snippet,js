
import React from 'react'
import { makeStyles } from '@material-ui/core'

const useStyles = makeStyles ((theme)=>({}));
 export default function Header () {
   const classes = useStyles();
   return (
    <div>
      appbar
    </div>
  )
 }


 <Collapse 
      in={checked}
      {... (checked ? { timeout: 1000} : {})}
      collapsedHeight="50"> <Collapse />